NODE_ENV=test
PORT=3334

DATABASE_URL=postgresql://docker:docker@localhost:5433/reloadcars_test

# Segredo apenas para testes locais; nunca use em produção.
BETTER_AUTH_SECRET=test-secret-only-for-local-tests-0123456789
BETTER_AUTH_URL=http://localhost:3334

WEB_ORIGIN=http://localhost:3000
MOBILE_SCHEME=reloadcars://

# Mapbox é simulado nos testes
MAPBOX_ACCESS_TOKEN=test-token
