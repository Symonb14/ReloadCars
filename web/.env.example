# URL da API (usada pelo proxy /backend do Next; nunca exposta ao navegador)
API_URL=http://localhost:3333

# Endereço público deste painel (usado pelo cliente de login)
NEXT_PUBLIC_APP_URL=http://localhost:3000
